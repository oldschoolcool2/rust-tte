% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{expand_trial_weighted_df}
\alias{expand_trial_weighted_df}
\title{Expand a cohort data.frame and attach pre-computed weights, in memory (wrapper)}
\usage{
expand_trial_weighted_df(
  cohort,
  factors,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "PP"
)
}
\arguments{
\item{cohort}{A \code{data.frame} (or tibble / \code{data.table} / Arrow \code{Table}) of long
person-time rows. Coerced with \code{as.data.frame()}.}

\item{factors}{A \code{data.frame} with columns \code{id}, \code{period}, \code{weight_factor}.
Coerced with \code{as.data.frame()}.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} or \code{"PP"}; selects the weight model upstream, but the
application arithmetic is identical for both.}
}
\value{
A \code{data.frame} with the six structural columns plus \code{weight}.
}
\description{
Frame-in / frame-out analogue of \code{\link[=expand_trial_weighted]{expand_trial_weighted()}}: takes an in-memory
cohort \code{data.frame} and a pre-computed factor \code{data.frame}
(\code{id}, \code{period}, \code{weight_factor}), and returns the weighted, expanded frame as a
\code{data.frame}. Wraps the extendr-generated \code{\link[=expand_weighted_df]{expand_weighted_df()}}. A
\code{bit64::integer64} \code{id} in either frame round-trips exactly.
}
\examples{
\dontrun{
weighted <- expand_trial_weighted_df(cohort, factors, estimand = "PP")
}
}
\seealso{
\code{\link[=expand_trial_weighted]{expand_trial_weighted()}} for the Parquet-path equivalent.
}
