% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{expand_trial_weighted_fitted_df}
\alias{expand_trial_weighted_fitted_df}
\title{Fit IPW weights and expand a cohort data.frame in one call, in memory (wrapper)}
\usage{
expand_trial_weighted_fitted_df(
  cohort,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "PP",
  switch_numerator = NULL,
  switch_denominator = NULL,
  censor_col = NULL,
  censor_numerator = NULL,
  censor_denominator = NULL,
  pool_censor = "none"
)
}
\arguments{
\item{cohort}{A \code{data.frame} (or tibble / \code{data.table} / Arrow \code{Table}) of long
person-time rows. Coerced with \code{as.data.frame()}.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} or \code{"PP"}.}

\item{switch_numerator, switch_denominator}{Character vectors of covariate column
names for the switching numerator / denominator models, or \code{NULL} to omit
switching weights.}

\item{censor_col}{Name of the \verb{\{0,1\}} censoring-indicator column; the modelled
response is \code{1 - censor_col}. \code{NULL} omits IPCW weights.}

\item{censor_numerator, censor_denominator}{Character vectors of covariate column
names for the IPCW numerator / denominator models.}

\item{pool_censor}{How the IPCW models are pooled across the previous-treatment
strata: \code{"none"}, \code{"numerator"}, or \code{"both"}.}
}
\value{
A \code{data.frame} with the six structural columns plus \code{weight}.
}
\description{
Frame-in / frame-out analogue of \code{\link[=expand_trial_weighted_fitted]{expand_trial_weighted_fitted()}}: takes a raw
cohort \code{data.frame} straight to a weighted, expanded \code{data.frame} in one call —
fitting the switching and/or IPCW models in Rust (no pre-computed factor table),
expanding under \code{estimand}, and accumulating the fitted factor into the
cumulative \code{weight}. The six structural columns are bit-exact; \code{weight} matches
the Oracle within the staged ~1e-6 tolerance. Wraps the extendr-generated
\code{\link[=expand_weighted_fitted_df]{expand_weighted_fitted_df()}}.
}
\details{
Model presence follows the same rule as \code{\link[=fit_trial_weights_df]{fit_trial_weights_df()}}.
}
\examples{
\dontrun{
weighted <- expand_trial_weighted_fitted_df(
  cohort, estimand = "PP",
  switch_numerator = "x2", switch_denominator = c("x2", "x1"),
  censor_col = "censored",
  censor_numerator = "x2", censor_denominator = c("x2", "x1"),
  pool_censor = "none"
)
}
}
\seealso{
\code{\link[=expand_trial_weighted_fitted]{expand_trial_weighted_fitted()}} for the Parquet-path equivalent;
\code{\link[=fit_trial_weights_df]{fit_trial_weights_df()}} to return only the factor table.
}
