% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/te-datastore-tters.R
\name{save_to_tters}
\alias{save_to_tters}
\title{Create a \code{te_datastore_tters} storage backend}
\usage{
save_to_tters()
}
\value{
A \code{te_datastore_tters} object with \code{N = 0L} and an empty data slot.
}
\description{
Constructor (the \verb{save_to_*} convention) for the Rust-backed \code{te_datastore}
subclass. Like the reference backends it does no work — it returns an empty
store to hand to \code{\link[TrialEmulation:set_expansion_options]{TrialEmulation::set_expansion_options()}}. The expansion is
run later by \code{\link[=expand_trials_tters]{expand_trials_tters()}}.
}
\details{
Requires the \code{TrialEmulation} (and \code{data.table}) package: the returned object
is an S4 subclass of \code{TrialEmulation}'s \code{te_datastore}, so the class only
exists when \code{TrialEmulation} is installed.
}
\examples{
\dontrun{
library(TrialEmulation)
trial_sequence("ITT") |>
  set_data(data = data_censored) |>
  set_outcome_model(adjustment_terms = ~x2) |>
  set_expansion_options(output = save_to_tters(), chunk_size = 0)
}
}
\seealso{
\code{\link[=expand_trials_tters]{expand_trials_tters()}} to populate it with a Rust-fast expansion.
}
\concept{save_to}
