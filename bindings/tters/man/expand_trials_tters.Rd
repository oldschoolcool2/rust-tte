% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/te-datastore-tters.R
\name{expand_trials_tters}
\alias{expand_trials_tters}
\title{Expand a sequence of target trials with the Rust + Polars engine}
\usage{
expand_trials_tters(object, fallback = TRUE, quiet = FALSE)
}
\arguments{
\item{object}{A configured \code{trial_sequence} (ITT or PP). The AT estimand is
not yet supported and falls back to R.}

\item{fallback}{If \code{TRUE} (default), any failure of the Rust path (including an
unsupported estimand or a missing toolchain) falls back to
\code{\link[TrialEmulation:expand_trials]{TrialEmulation::expand_trials()}} with a message. If \code{FALSE}, the error is
raised.}

\item{quiet}{If \code{TRUE}, suppress the fallback message.}
}
\value{
The updated \code{trial_sequence}, with its \verb{@expansion@datastore}
populated — the same object type \code{\link[TrialEmulation:expand_trials]{TrialEmulation::expand_trials()}} returns.
}
\description{
A drop-in replacement for \code{\link[TrialEmulation:expand_trials]{TrialEmulation::expand_trials()}} that runs the
expensive expansion in Rust (\code{tters}) instead of R, then stores the result
through the \code{trial_sequence}'s registered \code{te_datastore}. The produced frame
is byte-equivalent to the default path (structural columns bit-exact, \code{weight}
to within machine precision), so the downstream — \code{load_expanded_data()},
\code{sample_controls()}, \code{fit_msm()} — behaves identically.
}
\details{
Estimation stays entirely in R. Weight \emph{models} are fit by
\code{calculate_weights()}; this function reads that per-period \code{wt} verbatim and
Rust performs only the deterministic expansion and weight accumulation.

Set up the \code{trial_sequence} exactly as for \code{\link[TrialEmulation:expand_trials]{TrialEmulation::expand_trials()}}
(\code{set_data()} -> optional weight models + \code{calculate_weights()} ->
\code{set_outcome_model()} -> \code{set_expansion_options()}), then call this instead of
\code{expand_trials()}. The registered output may be \code{\link[=save_to_tters]{save_to_tters()}} or any other
\code{te_datastore} (e.g. \code{save_to_datatable()}); the speedup comes from the Rust
expansion, not the store.
}
\examples{
\dontrun{
library(TrialEmulation)
data("data_censored")
trial <- trial_sequence("ITT") |>
  set_data(data = data_censored) |>
  set_outcome_model(adjustment_terms = ~x2) |>
  set_expansion_options(output = save_to_tters(), chunk_size = 0)
trial <- expand_trials_tters(trial)
trial <- load_expanded_data(trial, seed = 1234, p_control = 0.5)
trial <- fit_msm(trial)
}
}
\seealso{
\code{\link[=save_to_tters]{save_to_tters()}}; \code{\link[TrialEmulation:expand_trials]{TrialEmulation::expand_trials()}}.
}
