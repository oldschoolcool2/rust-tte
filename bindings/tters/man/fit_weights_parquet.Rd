% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{fit_weights_parquet}
\alias{fit_weights_parquet}
\title{Fit the inverse-probability \strong{weight factor} for a Parquet cohort in Rust and
write the per-\verb{(id, period)} factor table (\verb{id, period, weight_factor}).}
\usage{
fit_weights_parquet(
  input_path,
  output_path,
  id_col,
  period_col,
  treatment_col,
  eligible_col,
  outcome_col,
  first_period,
  last_period,
  estimand,
  use_switch,
  switch_numerator,
  switch_denominator,
  use_censor,
  censor_col,
  censor_numerator,
  censor_denominator,
  pool_censor
)
}
\arguments{
\item{input_path}{Path to the input Parquet cohort (long person-time).}

\item{output_path}{Path where the \verb{(id, period, weight_factor)} table is written.}

\item{id_col, period_col, treatment_col}{Column names in the input.}

\item{eligible_col, outcome_col}{Eligibility / outcome column names.}

\item{first_period, last_period}{Inclusive integer bounds on \code{trial_period}.}

\item{estimand}{\code{"ITT"} or \code{"PP"}; per-protocol runs the artificial-censoring
state machine and (with switching covariates) the switch models.
Case-insensitive.}

\item{use_switch}{Whether to fit per-protocol switching-weight models.}

\item{switch_numerator, switch_denominator}{Covariate columns for the switching
numerator (stabiliser) and denominator models (ignored when \code{use_switch} is
\code{FALSE}).}

\item{use_censor}{Whether to fit inverse-probability-of-censoring (IPCW) models.}

\item{censor_col}{Name of the \verb{\{0,1\}} censoring-indicator column; the response
is \code{1 - censor_col} (ignored when \code{use_censor} is \code{FALSE}).}

\item{censor_numerator, censor_denominator}{Covariate columns for the IPCW
numerator/denominator models (ignored when \code{use_censor} is \code{FALSE}).}

\item{pool_censor}{How the IPCW models are pooled across the previous-treatment
strata: \code{"none"}, \code{"numerator"}, or \code{"both"}. Case-insensitive.}
}
\value{
\code{NULL}, invisibly; the factor table is written to \code{output_path}. Errors
in the core engine (including weight-fit failures) surface as R errors.
}
\description{
A thin FFI shim over \code{tte_expand::fit_weights_parquet} (the Phase-6
\code{weights-fit} surface). Unlike \code{expand_weighted_parquet()}, which \emph{applies} a
pre-computed factor table, this \emph{fits} the IPW models in Rust: it ports
\code{TrialEmulation}'s \code{data_manipulation} + \code{censor_func} design preparation and
binds a deterministic binomial-logit solver for the switching and/or IPCW
censoring models, then forms \code{wt = wt_switch * wtC}. The structural design is
exact; the fitted factors reproduce R \code{glm} within the staged ~1e-6 tolerance
(ADR-2), not bit-for-bit. Robust/sandwich variance and the marginal structural
model stay in R.
}
\examples{
\dontrun{
fit_weights_parquet(
  "cohort.parquet", "factors.parquet",
  "id", "period", "treatment", "eligible", "outcome",
  0L, .Machine$integer.max, "PP",
  TRUE, c("x2"), c("x2", "x1"),
  FALSE, "", character(0), character(0), "none"
)
}
}
