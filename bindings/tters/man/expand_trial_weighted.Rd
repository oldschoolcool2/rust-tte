% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{expand_trial_weighted}
\alias{expand_trial_weighted}
\title{Expand a dataset and attach pre-computed inverse-probability weights}
\usage{
expand_trial_weighted(
  input_path,
  factors_path,
  output_path,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "PP"
)
}
\arguments{
\item{input_path}{Path to an existing input Parquet file.}

\item{factors_path}{Path to the per-\verb{(id, period)} factor Parquet file.}

\item{output_path}{Path to write the weighted Parquet file.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} or \code{"PP"}; selects the weight model upstream, but the
application arithmetic is identical for both.}
}
\value{
\code{output_path}, invisibly.
}
\description{
User-facing wrapper around the extendr-generated \code{\link[=expand_weighted_parquet]{expand_weighted_parquet()}}.
It expands \code{input_path} under \code{estimand}, joins the per-\verb{(id, period)} factor
table at \code{factors_path} (\verb{id, period, weight_factor}), and writes the six
structural columns plus the cumulative-product \code{weight}. Weight \emph{values} are
produced upstream in R; the engine reproduces only their deterministic
accumulation.
}
\examples{
input <- system.file("extdata", "weights", "input_data_censored.parquet",
                     package = "tters")
factors <- system.file("extdata", "weights",
                       "input_data_censored_itt_weights.parquet",
                       package = "tters")
expand_trial_weighted(input, factors, tempfile(fileext = ".parquet"),
                      estimand = "ITT")
}
