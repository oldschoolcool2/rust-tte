% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{expand_trial_df}
\alias{expand_trial_df}
\title{Expand a target-trial cohort data.frame in memory (ergonomic wrapper)}
\usage{
expand_trial_df(
  cohort,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "ITT"
)
}
\arguments{
\item{cohort}{A \code{data.frame} (or tibble / \code{data.table} / Arrow \code{Table}) of long
person-time rows. Coerced with \code{as.data.frame()}.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} (intention-to-treat, no artificial censoring) or
\code{"PP"} (per-protocol, censor each trial at the first treatment deviation).}
}
\value{
A \code{data.frame} with the six structural columns
(\code{id}, \code{trial_period}, \code{followup_time}, \code{assigned_treatment}, \code{treatment},
\code{outcome}).
}
\description{
Frame-in / frame-out analogue of \code{\link[=expand_trial]{expand_trial()}}: takes an in-memory cohort
\code{data.frame} and returns the expanded trial frame as a \code{data.frame}, with no
intermediate Parquet. Wraps the extendr-generated \code{\link[=expand_df]{expand_df()}}.
}
\examples{
\dontrun{
cohort <- arrow::read_parquet("input.parquet")
expanded <- expand_trial_df(cohort, estimand = "PP")
}
}
\seealso{
\code{\link[=expand_trial]{expand_trial()}} for the Parquet-path equivalent.
}
