% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{fit_weights_df}
\alias{fit_weights_df}
\title{Fit the inverse-probability weight factor for an in-memory cohort and return the
per-\verb{(id, period)} factor table (\verb{id, period, weight_factor}) as a \code{data.frame}
— the frame-in/frame-out analogue of \code{fit_weights_parquet()}.}
\usage{
fit_weights_df(
  cohort,
  id_col,
  period_col,
  treatment_col,
  eligible_col,
  outcome_col,
  first_period,
  last_period,
  estimand,
  use_switch,
  switch_numerator,
  switch_denominator,
  use_censor,
  censor_col,
  censor_numerator,
  censor_denominator,
  pool_censor
)
}
\arguments{
\item{cohort}{An R \code{data.frame} of long person-time rows.}

\item{id_col, period_col, treatment_col}{Column names in \code{cohort}.}

\item{eligible_col, outcome_col}{Eligibility / outcome column names.}

\item{first_period, last_period}{Inclusive integer bounds on \code{trial_period}.}

\item{estimand}{\code{"ITT"} or \code{"PP"}. Case-insensitive.}

\item{use_switch}{Whether to fit per-protocol switching-weight models.}

\item{switch_numerator, switch_denominator}{Covariate columns for the switching
numerator/denominator models (ignored when \code{use_switch} is \code{FALSE}).}

\item{use_censor}{Whether to fit inverse-probability-of-censoring (IPCW) models.}

\item{censor_col}{Name of the \verb{\{0,1\}} censoring-indicator column; the response
is \code{1 - censor_col} (ignored when \code{use_censor} is \code{FALSE}).}

\item{censor_numerator, censor_denominator}{Covariate columns for the IPCW
numerator/denominator models (ignored when \code{use_censor} is \code{FALSE}).}

\item{pool_censor}{How the IPCW models are pooled across the previous-treatment
strata: \code{"none"}, \code{"numerator"}, or \code{"both"}. Case-insensitive.}
}
\value{
A \code{data.frame} with columns \code{id}, \code{period}, \code{weight_factor} (a 64-bit
integer \code{id} is returned as \code{bit64::integer64}). Errors in the core engine
(including weight-fit failures) surface as R errors.
}
\description{
Fit the inverse-probability weight factor for an in-memory cohort and return the
per-\verb{(id, period)} factor table (\verb{id, period, weight_factor}) as a \code{data.frame}
— the frame-in/frame-out analogue of \code{fit_weights_parquet()}.
}
