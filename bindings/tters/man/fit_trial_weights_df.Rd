% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{fit_trial_weights_df}
\alias{fit_trial_weights_df}
\title{Fit inverse-probability weights for a cohort data.frame, in memory (wrapper)}
\usage{
fit_trial_weights_df(
  cohort,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "PP",
  switch_numerator = NULL,
  switch_denominator = NULL,
  censor_col = NULL,
  censor_numerator = NULL,
  censor_denominator = NULL,
  pool_censor = "none"
)
}
\arguments{
\item{cohort}{A \code{data.frame} (or tibble / \code{data.table} / Arrow \code{Table}) of long
person-time rows. Coerced with \code{as.data.frame()}.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} or \code{"PP"}.}

\item{switch_numerator, switch_denominator}{Character vectors of covariate column
names for the switching numerator / denominator models, or \code{NULL} to omit
switching weights.}

\item{censor_col}{Name of the \verb{\{0,1\}} censoring-indicator column; the modelled
response is \code{1 - censor_col}. \code{NULL} omits IPCW weights.}

\item{censor_numerator, censor_denominator}{Character vectors of covariate column
names for the IPCW numerator / denominator models.}

\item{pool_censor}{How the IPCW models are pooled across the previous-treatment
strata: \code{"none"}, \code{"numerator"}, or \code{"both"}.}
}
\value{
A \code{data.frame} with columns \code{id}, \code{period}, \code{weight_factor}.
}
\description{
Frame-in / frame-out analogue of \code{\link[=fit_trial_weights]{fit_trial_weights()}}: fits the IPW
switching and/or IPCW censoring models in Rust from an in-memory cohort
\code{data.frame} and returns the per-\verb{(id, period)} factor table as a \code{data.frame}
(\code{id}, \code{period}, \code{weight_factor}) — the table \code{\link[=expand_trial_weighted_df]{expand_trial_weighted_df()}}
consumes. Wraps the extendr-generated \code{\link[=fit_weights_df]{fit_weights_df()}}.
}
\details{
Model presence follows the same \code{NULL}-driven rule as \code{\link[=fit_trial_weights]{fit_trial_weights()}}: a
switching model is fitted when either \verb{switch_*} covariate vector is non-\code{NULL};
an IPCW model is fitted when \code{censor_col} is non-\code{NULL}.
}
\examples{
\dontrun{
factors <- fit_trial_weights_df(
  cohort, estimand = "PP",
  switch_numerator = "x2", switch_denominator = c("x2", "x1")
)
}
}
\seealso{
\code{\link[=fit_trial_weights]{fit_trial_weights()}} for the Parquet-path equivalent;
\code{\link[=expand_trial_weighted_fitted_df]{expand_trial_weighted_fitted_df()}} to fit and expand in a single call.
}
