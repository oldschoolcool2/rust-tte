% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{expand_weighted_df}
\alias{expand_weighted_df}
\title{Expand an in-memory cohort and attach pre-computed inverse-probability weights,
returning the weighted frame as a \code{data.frame} — the frame-in/frame-out
analogue of \code{expand_weighted_parquet()}.}
\usage{
expand_weighted_df(
  cohort,
  factors,
  id_col,
  period_col,
  treatment_col,
  eligible_col,
  outcome_col,
  first_period,
  last_period,
  estimand
)
}
\arguments{
\item{cohort}{An R \code{data.frame} of long person-time rows.}

\item{factors}{An R \code{data.frame} with columns \code{id}, \code{period}, \code{weight_factor}.}

\item{id_col, period_col, treatment_col}{Column names in \code{cohort}.}

\item{eligible_col, outcome_col}{Eligibility / outcome column names.}

\item{first_period, last_period}{Inclusive integer bounds on \code{trial_period}.}

\item{estimand}{\code{"ITT"} or \code{"PP"}. Case-insensitive.}
}
\value{
A \code{data.frame} with the six structural columns plus \code{weight}. Errors in
the core engine surface as R errors.
}
\description{
Both the cohort and the per-\verb{(id, period)} factor table (\verb{id, period, weight_factor}) are passed as R \code{data.frame}s; the engine expands under
\code{estimand}, joins the factors, and accumulates the cumulative-product \code{weight}.
}
