% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{fit_trial_weights}
\alias{fit_trial_weights}
\title{Fit inverse-probability weights for a target-trial cohort (ergonomic wrapper)}
\usage{
fit_trial_weights(
  input_path,
  output_path,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "PP",
  switch_numerator = NULL,
  switch_denominator = NULL,
  censor_col = NULL,
  censor_numerator = NULL,
  censor_denominator = NULL,
  pool_censor = "none"
)
}
\arguments{
\item{input_path}{Path to an existing input Parquet cohort (long person-time).}

\item{output_path}{Path to write the \verb{(id, period, weight_factor)} Parquet.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} or \code{"PP"}. Per-protocol runs the artificial-censoring
state machine and the switching models; intention-to-treat skips both.}

\item{switch_numerator, switch_denominator}{Character vectors of covariate column
names for the switching numerator (stabiliser) / denominator models, or \code{NULL}
(the default) to omit switching weights.}

\item{censor_col}{Name of the \verb{\{0,1\}} censoring-indicator column; the modelled
response is \code{1 - censor_col}. \code{NULL} (the default) omits IPCW weights.}

\item{censor_numerator, censor_denominator}{Character vectors of covariate column
names for the IPCW numerator / denominator models.}

\item{pool_censor}{How the IPCW models are pooled across the previous-treatment
strata: \code{"none"}, \code{"numerator"}, or \code{"both"}.}
}
\value{
\code{output_path}, invisibly.
}
\description{
User-facing wrapper around the extendr-generated \code{\link[=fit_weights_parquet]{fit_weights_parquet()}} that
\emph{fits} the IPW switching and/or IPCW censoring models in Rust and writes the
per-\verb{(id, period)} factor table (\verb{id, period, weight_factor}) — the table
\code{\link[=expand_trial_weighted]{expand_trial_weighted()}} consumes. Unlike that pre-computed-factor path, here
the weight \emph{models} are fitted in Rust (the Phase-6 \code{weights-fit} surface): a
faithful port of \code{TrialEmulation}'s design preparation plus a deterministic
binomial-logit solver. Robust/sandwich variance and the marginal structural
model stay in R.
}
\details{
A switching model is fitted when either \code{switch_numerator} or
\code{switch_denominator} is non-\code{NULL}; an IPCW censoring model is fitted when
\code{censor_col} is non-\code{NULL}. Covariates are character vectors of column names;
\code{character(0)} (or \code{NULL}) yields an intercept-only model.
}
\examples{
\dontrun{
# Per-protocol switching weights (numerator ~ x2, denominator ~ x2 + x1):
fit_trial_weights(
  "cohort.parquet", "factors.parquet", estimand = "PP",
  switch_numerator = "x2", switch_denominator = c("x2", "x1")
)
}
}
\seealso{
\code{\link[=expand_trial_weighted_fitted]{expand_trial_weighted_fitted()}} to fit and expand in a single call.
}
