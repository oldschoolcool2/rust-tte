% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{expand_parquet}
\alias{expand_parquet}
\title{Expand a prepared person-time Parquet dataset into the sequential
target-trial layout and write the result to \code{output_path}.}
\usage{
expand_parquet(
  input_path,
  output_path,
  id_col,
  period_col,
  treatment_col,
  eligible_col,
  outcome_col,
  first_period,
  last_period,
  estimand
)
}
\arguments{
\item{input_path}{Path to the input Parquet file.}

\item{output_path}{Path where the expanded Parquet is written.}

\item{id_col, period_col, treatment_col}{Column names in the input.}

\item{eligible_col, outcome_col}{Eligibility / outcome column names
(\code{TrialEmulation} defaults are \code{"eligible"} / \code{"outcome"}).}

\item{first_period, last_period}{Inclusive integer bounds on \code{trial_period}.}

\item{estimand}{\code{"ITT"} (no artificial censoring) or \code{"PP"} (per-protocol,
censor each trial at the first treatment deviation). Case-insensitive.}
}
\value{
\code{NULL}, invisibly; the expansion is written to \code{output_path}. Errors
in the core engine surface as R errors.
}
\description{
This is a thin FFI shim. All dtype-exact, deterministic Polars work lives in
the \code{tte_expand} core crate (which is \verb{#![forbid(unsafe_code)]}). The binding
crate cannot forbid unsafe because the extendr macros emit the FFI registrar.
Every \code{tte_expand::ExpandError} is mapped to an R error condition.
}
\examples{
\dontrun{
expand_parquet(
  "input.parquet", "expanded.parquet",
  "id", "period", "treatment", "eligible", "outcome",
  0L, .Machine$integer.max, "ITT"
)
}
}
