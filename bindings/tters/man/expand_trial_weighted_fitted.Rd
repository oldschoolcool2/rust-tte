% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{expand_trial_weighted_fitted}
\alias{expand_trial_weighted_fitted}
\title{Fit IPW weights and expand a cohort into a weighted trial frame (ergonomic wrapper)}
\usage{
expand_trial_weighted_fitted(
  input_path,
  output_path,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "PP",
  switch_numerator = NULL,
  switch_denominator = NULL,
  censor_col = NULL,
  censor_numerator = NULL,
  censor_denominator = NULL,
  pool_censor = "none"
)
}
\arguments{
\item{input_path}{Path to an existing input Parquet cohort (long person-time).}

\item{output_path}{Path to write the weighted, expanded Parquet.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} or \code{"PP"}. Per-protocol runs the artificial-censoring
state machine and the switching models; intention-to-treat skips both.}

\item{switch_numerator, switch_denominator}{Character vectors of covariate column
names for the switching numerator (stabiliser) / denominator models, or \code{NULL}
(the default) to omit switching weights.}

\item{censor_col}{Name of the \verb{\{0,1\}} censoring-indicator column; the modelled
response is \code{1 - censor_col}. \code{NULL} (the default) omits IPCW weights.}

\item{censor_numerator, censor_denominator}{Character vectors of covariate column
names for the IPCW numerator / denominator models.}

\item{pool_censor}{How the IPCW models are pooled across the previous-treatment
strata: \code{"none"}, \code{"numerator"}, or \code{"both"}.}
}
\value{
\code{output_path}, invisibly.
}
\description{
User-facing wrapper around the extendr-generated
\code{\link[=expand_weighted_fitted_parquet]{expand_weighted_fitted_parquet()}}. It takes a raw person-time cohort straight
to a weighted, expanded trial frame in one call — fitting the switching and/or
IPCW models in Rust (no pre-computed factor table), expanding under \code{estimand},
and accumulating the fitted factor into the cumulative \code{weight}. The six
structural columns are bit-exact; \code{weight} matches the Oracle within the staged
~1e-6 tolerance. Robust/sandwich variance and the marginal structural model
stay in R.
}
\details{
Model presence follows the same rule as \code{\link[=fit_trial_weights]{fit_trial_weights()}}: a switching model
is fitted when either \verb{switch_*} covariate vector is non-\code{NULL}; an IPCW model is
fitted when \code{censor_col} is non-\code{NULL}.
}
\examples{
\dontrun{
# Per-protocol switch + IPCW censoring, raw cohort to weighted frame in one call:
expand_trial_weighted_fitted(
  "cohort.parquet", "weighted.parquet", estimand = "PP",
  switch_numerator = "x2", switch_denominator = c("x2", "x1"),
  censor_col = "censored",
  censor_numerator = "x2", censor_denominator = c("x2", "x1"),
  pool_censor = "none"
)
}
}
\seealso{
\code{\link[=fit_trial_weights]{fit_trial_weights()}} to write only the \verb{(id, period, weight_factor)}
factor table.
}
