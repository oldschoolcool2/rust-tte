% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tters-package.R
\name{expand_trial}
\alias{expand_trial}
\title{Expand a target-trial person-time dataset (ergonomic wrapper)}
\usage{
expand_trial(
  input_path,
  output_path,
  id_col = "id",
  period_col = "period",
  treatment_col = "treatment",
  eligible_col = "eligible",
  outcome_col = "outcome",
  first_period = 0L,
  last_period = .Machine$integer.max,
  estimand = "ITT"
)
}
\arguments{
\item{input_path}{Path to an existing input Parquet file.}

\item{output_path}{Path to write the expanded Parquet file.}

\item{id_col, period_col, treatment_col, eligible_col, outcome_col}{Column names.
Defaults match the TrialEmulation conventions.}

\item{first_period, last_period}{Inclusive integer period bounds.}

\item{estimand}{\code{"ITT"} (intention-to-treat, no artificial censoring) or
\code{"PP"} (per-protocol, censor each trial at the first treatment deviation).}
}
\value{
\code{output_path}, invisibly.
}
\description{
User-facing wrapper around the extendr-generated \code{\link[=expand_parquet]{expand_parquet()}} that
validates inputs and uses sensible defaults. The heavy lifting happens in the
Rust core crate.
}
\examples{
\dontrun{
expand_trial("input.parquet", "expanded.parquet", estimand = "PP")
}
}
