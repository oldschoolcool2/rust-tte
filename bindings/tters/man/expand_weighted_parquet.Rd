% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{expand_weighted_parquet}
\alias{expand_weighted_parquet}
\title{Expand a person-time Parquet dataset and attach pre-computed
inverse-probability weights, writing the weighted frame to \code{output_path}.}
\usage{
expand_weighted_parquet(
  input_path,
  factors_path,
  output_path,
  id_col,
  period_col,
  treatment_col,
  eligible_col,
  outcome_col,
  first_period,
  last_period,
  estimand
)
}
\arguments{
\item{input_path}{Path to the input Parquet file.}

\item{factors_path}{Path to the per-\verb{(id, period)} factor Parquet
(\verb{id, period, weight_factor}).}

\item{output_path}{Path where the weighted Parquet is written.}

\item{id_col, period_col, treatment_col}{Column names in the input.}

\item{eligible_col, outcome_col}{Eligibility / outcome column names.}

\item{first_period, last_period}{Inclusive integer bounds on \code{trial_period}.}

\item{estimand}{\code{"ITT"} or \code{"PP"}; selects the weight \emph{model} upstream, but the
application arithmetic (join + cumulative product) is identical for both.}
}
\value{
\code{NULL}, invisibly; the weighted expansion is written to \code{output_path}.
Errors in the core engine surface as R errors.
}
\description{
A thin FFI shim over \code{tte_expand::expand_weighted_parquet}: it expands the
input under \code{estimand}, joins the per-\verb{(id, period)} factor table at
\code{factors_path} (\verb{id, period, weight_factor}), and writes the six structural
columns plus the cumulative-product \code{weight}. The weight \emph{values} come from R
(the \code{glm} fit); the engine only reproduces their deterministic accumulation.
}
\examples{
\dontrun{
expand_weighted_parquet(
  "input.parquet", "factors.parquet", "weighted.parquet",
  "id", "period", "treatment", "eligible", "outcome",
  0L, .Machine$integer.max, "PP"
)
}
}
