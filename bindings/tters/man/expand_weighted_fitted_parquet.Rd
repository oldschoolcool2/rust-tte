% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{expand_weighted_fitted_parquet}
\alias{expand_weighted_fitted_parquet}
\title{Fit the IPW weights in Rust, expand the cohort, apply the weights, and write
the weighted trial frame — a raw cohort to a weighted, expanded frame in one
call (no pre-computed factor table).}
\usage{
expand_weighted_fitted_parquet(
  input_path,
  output_path,
  id_col,
  period_col,
  treatment_col,
  eligible_col,
  outcome_col,
  first_period,
  last_period,
  estimand,
  use_switch,
  switch_numerator,
  switch_denominator,
  use_censor,
  censor_col,
  censor_numerator,
  censor_denominator,
  pool_censor
)
}
\arguments{
\item{input_path}{Path to the input Parquet cohort (long person-time).}

\item{output_path}{Path where the weighted, expanded Parquet is written.}

\item{id_col, period_col, treatment_col}{Column names in the input.}

\item{eligible_col, outcome_col}{Eligibility / outcome column names.}

\item{first_period, last_period}{Inclusive integer bounds on \code{trial_period}.}

\item{estimand}{\code{"ITT"} or \code{"PP"}. Case-insensitive.}

\item{use_switch}{Whether to fit per-protocol switching-weight models.}

\item{switch_numerator, switch_denominator}{Covariate columns for the switching
numerator/denominator models (ignored when \code{use_switch} is \code{FALSE}).}

\item{use_censor}{Whether to fit inverse-probability-of-censoring (IPCW) models.}

\item{censor_col}{Name of the \verb{\{0,1\}} censoring-indicator column; the response
is \code{1 - censor_col} (ignored when \code{use_censor} is \code{FALSE}).}

\item{censor_numerator, censor_denominator}{Covariate columns for the IPCW
numerator/denominator models (ignored when \code{use_censor} is \code{FALSE}).}

\item{pool_censor}{How the IPCW models are pooled across the previous-treatment
strata: \code{"none"}, \code{"numerator"}, or \code{"both"}. Case-insensitive.}
}
\value{
\code{NULL}, invisibly; the weighted expansion is written to \code{output_path}.
Errors in the core engine (including weight-fit failures) surface as R errors.
}
\description{
A thin FFI shim over \code{tte_expand::expand_weighted_fitted_parquet}: the fully
in-Rust analogue of \code{expand_weighted_parquet()}. It fits the switching and/or
IPCW models from the spec (as \code{fit_weights_parquet()} does), expands under
\code{estimand}, joins and accumulates the fitted factor, and writes the six
structural columns plus the cumulative-product \code{weight}. Structural columns are
bit-exact; \code{weight} matches the Oracle within the staged ~1e-6 tolerance
(ADR-2).
}
\examples{
\dontrun{
expand_weighted_fitted_parquet(
  "cohort.parquet", "weighted.parquet",
  "id", "period", "treatment", "eligible", "outcome",
  0L, .Machine$integer.max, "PP",
  TRUE, c("x2"), c("x2", "x1"),
  FALSE, "", character(0), character(0), "none"
)
}
}
